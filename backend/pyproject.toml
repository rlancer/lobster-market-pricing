[project]
name = "options-screener-backend"
version = "0.1.0"
description = "Free S&P 500 options screener backend (download + DuckDB + API)"
requires-python = ">=3.11"
dependencies = [
    "yfinance>=0.2.40",
    "duckdb>=1.1.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pandas>=2.0",
    "requests>=2.31",
    "lxml>=5.0",
]

[tool.uv]
package = false