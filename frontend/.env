# The screener API Worker. Points the frontend at the deployed Cloudflare
# Worker (R2 SQL backend). For local dev against `wrangler dev`, set this to
# http://127.0.0.1:8787 — or leave empty and use the Vite `/api` proxy.
VITE_API_BASE=https://screener-api.robertlancer.workers.dev
