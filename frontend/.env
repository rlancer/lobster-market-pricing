# Public R2 bucket (r2.dev subdomain) serving the Parquet dataset.
# Consumed by src/db.ts (PARQUET_BASE). The bucket has CORS enabled for
# cross-origin range reads from the Pages origin. Real data lives in R2;
# frontend/public/options/ is gitignored local-dev-only and has been removed.
VITE_R2_BASE=https://pub-cbc7194a67fb4abf9b3ce502efed171a.r2.dev/options
